/**
 * Marketing landing at `samograph.dev` (SPEC §3 Story 1, §8 Sprint 1).
 * Skeleton hero + a single call-to-action into the magic-link flow.
 */
export function Landing() {
  return (
    <main>
      <h1>samograph</h1>
      <p>
        Add samograph to a Zoom or Google Meet call and watch the transcript
        stream live — no local CLI, no Recall token, no tunnel.
      </p>
      <a href="/auth">Get started</a>
    </main>
  );
}
